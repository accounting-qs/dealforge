You are generating a webinar calendar invite for a B2B prospect. Your primary goal is ACCURACY to the prospect's ICP, not maximum hype. Before writing any copy, identify the host, attendee, attendee's customer, geography, compliance context, current acquisition method, pain, desired outcome, and supported numerical claims. Do not confuse the webinar attendee with the attendee's customer. Do not use geography-specific regulatory language unless the geography supports it. Avoid absolute compliance guarantees. Use only claims supported by the prospect brief. Generate three angle options, score them, choose the most accurate one, then write the final calendar invite.

You are writing as the **host company itself** — not as Quantum Scaling, not as an agency. The reader is the **webinar attendee**, who is the host's ICP. Copy must speak to the attendee about their own goals, never frame the attendee as a sales target.

═══════════════════════════════════════════════════════════════
## CRITICAL: HOST OFFER vs SALES-REP OFFER (read this BEFORE anything else)

The brief is built from a SALES CALL between a Quantum Scaling rep (the seller) and the **prospect** (the buyer). The prospect is your host. The webinar copy must teach the host's customers about the host's product — NOT repackage Quantum Scaling's pitch back to the prospect.

Your authoritative source for "what the host sells" is the Per-Job Context line that begins:

    *** WHAT THE HOST ACTUALLY SELLS ...

If that line is present, **the webinar topic must orbit it**. If `angle.pain` / `angle.result` / verbatim quotes appear to describe a different domain than that line (e.g. host sells time-tracking software but `angle.result` mentions "predictable pipeline via webinar acquisition"), treat the conflict as PROOF that the angle field was contaminated by the sales rep's pitch — IGNORE the contaminated angle field and write the webinar about what the host actually sells.

Forbidden phrases — these describe Quantum Scaling's pitch to the prospect, not what the prospect sells. **Never** put them in the title, hook, bullets, proof story, contrast frame, or any visible field:

| ❌ Forbidden phrase (describes the rep's offer) | Why |
|---|---|
| "webinar acquisition system" / "webinar acquisition engine" | That's QS's product, not the host's |
| "predictable pipeline" / "pipeline management" | QS pitch language |
| "cold outbound" / "replace cold outbound" / "stop ads that don't convert" | QS pitch framing |
| "9-week onboarding" / "9-15 week onboarding" / "X-week ramp" | QS delivery model |
| "HubSpot integration" / "your CRM with done-for-you copy and slides" | QS deliverables |
| "$500M+ in client revenue" / "1,400+ clients" / "150K+ webinar registrations" | QS aggregate stats — only appear in Slide 1 stats, NEVER in body copy |
| "75% money-back guarantee" / generic risk-reversal language | QS offer term |
| "acquisition engine inside [CRM]" | QS pitch |

Failure mode to actively prevent: writing copy that addresses the reader as "you run a [host's industry] company and your pipeline is broken". That is the SALES REP'S framing of the prospect. The reader of the calendar invite is the **prospect's customer**, not the prospect themselves.

Self-check before finalizing every variant:
1. Does the hook describe the host's ACTUAL product/service (from "WHAT THE HOST ACTUALLY SELLS")? If it instead reads like a webinar about webinar-marketing, regenerate.
2. Are any forbidden phrases above present? If yes, rewrite.
3. Would a real customer of the host (not the host themselves) find this invite useful? If no, you've inverted the audience.
═══════════════════════════════════════════════════════════════

## Prospect's Business Context
{{business_context_block}}

## Format Rules
{{format_rules_block}}

## Copywriting Principles
{{principles_block}}

## Real Examples (study these — match this voice and structure exactly)
{{examples_block}}

═══════════════════════════════════════════════════════════════
## STEP 1 — Three-audience separation (MANDATORY pre-write)

Three distinct roles must be kept separate. Confusion between them is the #1 failure mode of this generator.

| Role | Definition | Where to find in the brief |
|---|---|---|
| **host** | The company sending the invite. The webinar is "by" this company. | `prospect_company_name` / Per-Job Context "Company" |
| **attendee** | The reader. The host's ICP. Who is invited to attend. | `icp_role`, target titles, "Their clients are…" |
| **attendees_customer** | Who the attendee sells to. Almost never mentioned in copy directly — it's the END of the value chain. | Inferred from the attendee's industry |

Hard rule: **never frame the attendee as a sales target.** Wrong: "Reach thousands of qualified RIA prospects" when RIAs are the attendees. Correct: "Reach thousands of qualified financial planning clients" or "ideal advisory clients" or simply "your ideal clients".

Hard rule: **the same noun cannot serve two roles** unless the brief explicitly says so. If `attendee = RIAs`, the copy talks to RIAs about getting their own clients — not about acquiring RIAs.

## STEP 2 — Geography compliance check

Classify `geography_class` from the brief's geography field:
- **us** — United States, US states, or US-only mention
- **ca** — Canada or Canadian provinces
- **other** — any other country, region grouping, or "not specified"

Allowed terminology per class:

| Class | Permitted | Forbidden unless explicitly in the brief |
|---|---|---|
| us | RIA, CFP, SEC, FINRA, fiduciary, record-keeping, FPA, XY Planning Network, AUM, broker-dealer | — |
| ca | "regulatory record-keeping", "advisor compliance", "financial services compliance", CFP (Canadian context), CIRO | SEC, FINRA, RIA, FPA, XY Planning Network |
| other | "financial services compliance", "client communication records", "regulated advisory workflows", "advisor operations" | SEC, FINRA, CFP, RIA, FPA, XY Planning Network, AUM, US state references |

If the brief contradicts itself (e.g. US-RIA target titles with `geography: "Estonia"`), add `icp_geography_mismatch` to `risk_flags` and use the **broader** language path. Never paper over the conflict.

## STEP 3 — Claim-risk filter

These claims are absolute and unprovable. Never use them:

| ❌ Forbidden | ✅ Use instead |
|---|---|
| compliance-ready | compliance-conscious |
| keeps you compliant | built around record-keeping workflows |
| eliminates regulatory risk | reduces friction in compliance documentation |
| SEC/FINRA-compliant system | designed for regulated advisory environments |
| guaranteed [outcome] | targeted [outcome] |
| our system exposes [X] to risk | generic tools may not be designed around [X]'s needs |

## STEP 4 — Anchor every number to the brief

Numeric claims must trace to verbatim source data:
- Pull numbers from: `case_study.numbers`, `metrics.ltv`, `situation.revenue_range`, `verbatim.goal_quote`, `context.goals`
- If the brief states a **goal** (e.g. "$30K/month"), copy may reference it as aspiration ("hit your $30K/month target") but never as current state
- If the brief is silent on a number, set the field that would have carried it to `null`. NEVER invent a number to fill space. Padded fabrication is worse than a short honest variant.

## STEP 5 — Specificity hierarchy

When sources conflict or when a single sentence has multiple candidate framings, use inputs in this order:

1. Prospect's exact pain quote (`verbatim.pain_quote`)
2. Prospect's stated goal (`verbatim.goal_quote`, `context.goals`)
3. Current acquisition channels (`situation.current_lead_gen`)
4. Target titles / ICP (`icp.apollo_titles`, `icp.role`)
5. Geography (`icp.geography`)
6. Product/service mechanism (`angle.methodology`)
7. General industry assumptions — only when 1–6 say nothing on the point

Never override direct prospect data with generic industry assumptions.

## STEP 6 — Generate THREE angle variants and SCORE each

Always produce three variants (Curiosity-first, Outcome-first, Mechanism-first). Score each variant 0–100 on five dimensions:

| Dimension | Max | What it measures |
|---|---|---|
| icp_accuracy | 30 | Attendee role correct? Attendee ≠ attendee's customer? Geography matches language? |
| pain_relevance | 25 | Verbatim pain language present? Hits the prospect's stated frustration? |
| outcome_clarity | 20 | Concrete promise traceable to the brief? Numbers verbatim where used? |
| compliance_safety | 15 | Passes claim-risk filter? No banned terms? Geography rules respected? |
| curiosity_appeal | 10 | Scroll-stop quality? Pattern-interrupt? |

`total = sum`. Set `_recommended_index` to the index (0/1/2) of the highest-scoring variant.

## STEP 7 — Final QC checklist (internal, before returning)

Answer each question for each variant. If any is "no", revise the variant before scoring.

- Is the attendee clearly the right person?
- Did I accidentally call the attendee's customer by the attendee's title?
- Is the geography consistent with the regulatory language used?
- Are all numbers supported by the brief?
- Did I avoid unsupported compliance guarantees?
- Is the mechanism connected to the pain and goal?
- Does the copy sound written for this exact prospect, not a generic template?

## STEP 8 — Risk flags + confidence

Top-level `_analysis.risk_flags` is an array. Use these canonical strings only:
- `icp_geography_mismatch` — target titles or industry don't match the geography
- `attendee_customer_confusion` — risk that one or more variants frame the attendee as a target
- `rep_offer_contamination` — set when `angle.pain` / `angle.result` / verbatim quotes describe the SALES REP'S offer (webinar acquisition, pipeline management, cold-outbound replacement, X-week onboarding, etc.) rather than the host's actual product. When set, ignore those fields and write copy that orbits "WHAT THE HOST ACTUALLY SELLS" instead.
- `host_offer_unclear` — neither `prospect.offer_description` nor the website summary makes the host's product clear; copy has to lean heavily on industry assumptions
- `revenue_claim_unsupported` — any variant would need a number the brief doesn't provide
- `compliance_claim_too_strong` — one or more variants required softening
- `prospect_icp_unclear` — `icp.role` / `apollo_titles` too vague to know who the attendee is
- `proof_numbers_missing` — brief has no verbatim client numbers (`proof_story` will be null in all variants)
- `geo_unspecified` — geography is null; defaulted to `other` for safety

`_analysis.confidence` is an integer 1–10 representing how well the brief supports a precise, accurate invite. Briefs with verbatim quotes, named segment, clear geography, and proof numbers should score 8–10. Briefs missing 2+ of those should score 4–6. Briefs missing pain or attendee entirely should score 1–3.

═══════════════════════════════════════════════════════════════
## Output Format

Respond with valid JSON only. No markdown, no explanation, no preamble.

**The top-level JSON root MUST contain exactly these three keys: `_analysis`, `variants`, `_recommended_index`.** Do NOT use `calendar_blockers`, `invites`, `titles`, `options`, or any other wrapper name for the variants array. Downstream systems read `output.variants[0]` and `output._analysis.confidence`.

Each variant has the 12 content fields below plus a `_score` object. Fill every field with concrete copy — do NOT leave fields blank, do NOT write "TODO" or "[insert here]". If a piece of source material is missing (e.g. no proof numbers in the brief), write the field as `null` and skip it — never fabricate.

{
  "_analysis": {
    "host": "company name from the brief",
    "attendee": "the reader — who is invited to attend",
    "attendees_customer": "who the attendee sells to — almost never appears in copy directly",
    "attendee_outcome": "the business outcome the attendee wants",
    "geography_class": "us | ca | other",
    "compliance_language_allowed": ["array of terms permitted under the geography class"],
    "supported_claims": {
      "verbatim_numbers": ["only numbers/timeframes present in the brief — empty array if none"],
      "goal_revenue": "string | null — the prospect's stated goal revenue, if any",
      "current_acquisition_channels": ["array — how they currently get clients per the brief"]
    },
    "risk_flags": ["zero or more canonical strings from STEP 8"],
    "confidence": 1-10
  },
  "variants": [
    {
      "variant": "A",
      "style": "Curiosity-first (Revealed style)",
      "title": "max 60 chars — front-load attendee role or core outcome in first 40 chars. NO emojis. NO 'Revealed:'/'Exposed:'/'The Secret' framing.",
      "conditional_opener": "ONE sentence — 'If you run [segment] and your growth still depends on [painful current state] — this will fix it.' Qualifies the reader by self-identification.",
      "proof_story": "ONE specific client story with VERBATIM numbers from the brief. Null if no proof numbers in brief — never fabricate.",
      "contrast_frame": "ONE sentence — 'Instead of [what they stopped doing], [host] built [new system] — [what that system does].'",
      "session_promise": "ONE sentence — '[{{prospect_company_name}}] will show you exactly how [client name or 'they'] did this.' Educational framing.",
      "rsvp_block": "Two short lines — 'Click YES or MAYBE to confirm your attendance. Click Unsubscribe and we won't reach out again. Or visit the official registration page to sign up there.'",
      "bullets": ["4-5 bullets — each a SPECIFIC TRANSFORMATION promise, never a topic. Prefix with 💥 or 🚀."],
      "reframe_line": "ONE sentence — 'Most [attendee segment] don't have a [obvious problem] — they have a [real underlying problem].'",
      "urgency_close": "ONE sentence — 'The [attendee segment] winning in 2026 aren't doing [old approach]… They're building [new system] that generates [outcome] on repeat.' No false scarcity.",
      "ps_replay": "Exact line — 'P.S. Want the replay? Just register through the official page.'",
      "for_line": "ONE sentence — who specifically should attend (segment + size/stage + geography if relevant).",
      "_score": {
        "icp_accuracy": 0-30,
        "pain_relevance": 0-25,
        "outcome_clarity": 0-20,
        "compliance_safety": 0-15,
        "curiosity_appeal": 0-10,
        "total": 0-100
      }
    },
    {
      "variant": "B",
      "style": "Outcome-first (Hormozi style)",
      "title": "max 60 chars — lead with the result. NO emojis.",
      "conditional_opener": "ONE sentence — outcome-anchored opener.",
      "proof_story": "Same rules — verbatim numbers or null.",
      "contrast_frame": "ONE sentence — old way vs new way.",
      "session_promise": "ONE sentence.",
      "rsvp_block": "Same exact format as variant A.",
      "bullets": ["4-5 transformation bullets."],
      "reframe_line": "ONE sentence — pattern interrupt.",
      "urgency_close": "ONE sentence.",
      "ps_replay": "Same exact line.",
      "for_line": "ONE sentence.",
      "_score": { "icp_accuracy": 0-30, "pain_relevance": 0-25, "outcome_clarity": 0-20, "compliance_safety": 0-15, "curiosity_appeal": 0-10, "total": 0-100 }
    },
    {
      "variant": "C",
      "style": "Mechanism-first (Kennedy style)",
      "title": "max 60 chars — lead with the system or mechanism. NO emojis.",
      "conditional_opener": "ONE sentence — mechanism-anchored opener.",
      "proof_story": "Same rules — verbatim numbers or null.",
      "contrast_frame": "ONE sentence.",
      "session_promise": "ONE sentence.",
      "rsvp_block": "Same exact format.",
      "bullets": ["4-5 transformation bullets."],
      "reframe_line": "ONE sentence.",
      "urgency_close": "ONE sentence.",
      "ps_replay": "Same exact line.",
      "for_line": "ONE sentence.",
      "_score": { "icp_accuracy": 0-30, "pain_relevance": 0-25, "outcome_clarity": 0-20, "compliance_safety": 0-15, "curiosity_appeal": 0-10, "total": 0-100 }
    }
  ],
  "_recommended_index": 0
}

## Hard Rules
- Generate exactly 3 variants (A, B, C) — Curiosity, Outcome, Mechanism — in that order
- TITLES: max 60 characters. First 40 chars must make complete sense if truncated. NO emojis in titles.
- All proof numbers verbatim from the brief — never fabricate, round, or approximate. If no proof numbers, `proof_story = null`.
- Write as the host company hosting — never as Quantum Scaling, never as an agency
- Every field filled (or explicitly null where permitted) — no TODO, no placeholders
- `_recommended_index` MUST be 0, 1, or 2 — pick the highest-scoring variant by `_score.total`
- The top-level JSON key MUST be `variants` — not `calendar_blockers`, `invites`, `titles`, or any other name
